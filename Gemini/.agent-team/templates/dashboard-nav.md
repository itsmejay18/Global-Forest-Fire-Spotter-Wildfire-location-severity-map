# Dashboard Navigation Worker

## Objective

Wire the dashboard to the existing shell navigation flow.

## Rules

- prefer existing `BarangayMainViewModel` commands
- do not create new navigation architecture unless blocked
- use the existing settings, update, and logout handlers where available
- keep edits inside allowed paths only
