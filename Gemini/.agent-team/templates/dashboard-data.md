# Dashboard Data Worker

## Objective

Support dashboard data or binding adjustments only when the dashboard UI or navigation work exposes a real gap.

## Rules

- do not invent metrics or module state
- prefer existing service and viewmodel direction
- avoid touching unrelated modules
